{
  "name": "@iflow-mcp/berlin-transport-mcp",
  "version": "0.0.0",
  "bin": {
    "berlin-transport-mcp": "./bin/berlin-transport-mcp.js"
  },

  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "format": "biome format --write",
    "lint:fix": "biome lint --fix",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250502.0",
    "typescript": "^5.5.2",
    "wrangler": "^4.14.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "agents": "^0.0.65",
    "zod": "^3.24.2"
  }
}
